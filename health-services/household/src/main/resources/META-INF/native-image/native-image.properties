# Native image properties for household service
# Spring Boot will auto-generate most configurations during AOT processing
Args = --no-fallback \
       --enable-url-protocols=http,https \
       -H:+ReportExceptionStackTraces