# ============================================================================
#  Project service — load test configuration
#  Fill in the values below for your Dev/QA environment, then run ./run.sh
#  Every field marked  <<< FILL >>>  must be set for creates to pass validation.
# ============================================================================

# --- Target host (Dev/QA). No trailing slash. Context path /project is added by the plan. ---
# Example: https://health-dev.digit.org
host=https://unified-dev.digit.org/
protocol=https
port=443

# --- Auth & tenant (you said you have these) ---
authToken=eae7314a-8213-4dd3-8a95-db34ffab5621
tenantId=dev

# --- Project create fields (needed so the setup create succeeds; values must exist in dev MDMS/boundary) ---
projectType=<<< FILL: a projectType code valid in dev MDMS >>>
department=<<< FILL: e.g. DEPT_1 >>>
natureOfWork=<<< FILL: e.g. Other >>>
boundaryType=<<< FILL: e.g. Village >>>
boundary=<<< FILL: a boundary code valid in dev >>>

# --- Mapping refs you said you'll share (mapped to the whole project for testing) ---
userId=3d4482e2-abe8-4723-9f97-03316f13fab7
facilityId=F-2026-07-09-018215
# Optional — leave blank to let those creates be skipped/tolerated
beneficiaryId=
productId=PVAR-2026-03-26-000210

bf55681d-82b3-42b1-b51f-58ca72d9afb4

# ============================================================================
#  Load profile — Smooth ramp 0 -> 50 over the env (Light ceiling: max 50)
#  Tune these without touching the .jmx.
# ============================================================================
# Peak concurrent virtual users (the ceiling you approved)
peakUsers=50
# Seconds to ramp from 0 to peakUsers
rampSeconds=120
# Seconds to hold at peak after ramp
holdSeconds=120

# --- End-to-end persistence probe (poll _search after a write until row appears) ---
# How many times to poll and the gap between polls (ms)
persistencePollTries=20
persistencePollGapMs=500
