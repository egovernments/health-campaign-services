-- Drop unused tables that are no longer needed
-- These tables were created earlier but are not being used in the current implementation

-- Drop eg_cm_resource_activity table if it exists
DROP TABLE IF EXISTS eg_cm_resource_activity;

-- Drop eg_cm_campaign_process table if it exists  
DROP TABLE IF EXISTS eg_cm_campaign_process;